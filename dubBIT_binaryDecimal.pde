//TODO binary to decimal converter (sonst writeTable!! fertig)
// BINARY <> DECIMAL
// pow(2, i) pow(10, -i)


/* 
// binary <> decimal
public void decimalToBinary() { // * 8
  // string join (nf() nfs()
  int sum
  for(int i = 0; i < 8; i++) { // * 8
    sum += ### * pow(10, i);
    string join
  }
}

public void binaryToDecimal() { // * 8
  // string join (nf() nfs()
  int sum
  for(int i = 0; i < 8; i++) { // * 8
    sum += ### * pow(2, i);
    string join
  }
}


// get decimal
for (i = 0; i < 8; i++) {
  decimal = decimal64map.getInt[i];    
  binary = binaryMap.getString[i];  
}




*/
